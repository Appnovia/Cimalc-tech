"use client";
import Link from "next/link";
import { useEffect, useState } from "react";
import { AnimatePresence, motion } from "motion/react";
import { ArrowRight } from "lucide-react";
import { Button } from "../ui/botton";
const slides = [{ key: "headphones", title: "Find something you’ll love listening to.", copy: "From quiet mornings to long journeys, find headphones that feel right for the way you spend your time.", href: "/categories/headphones", label: "Headphones" }, { key: "laptops", title: "Make space for your next idea.", copy: "Find a laptop that can keep up with the way you work, study, and create.", href: "/categories/laptops", label: "Laptops" }, { key: "smart-home", title: "Make everyday life a little easier.", copy: "Discover simple smart-home upgrades that make your space more comfortable and connected.", href: "/categories/smart-home", label: "Smart home" }, { key: "accessories", title: "The little things matter too.", copy: "Find useful accessories that keep your favourite devices ready for whatever the day brings.", href: "/categories/accessories", label: "Accessories" }];
export function Hero() { const [active, setActive] = useState(0); const slide = slides[active]; useEffect(() => { const timer = window.setInterval(() => setActive((value) => (value + 1) % slides.length), 6500); return () => window.clearInterval(timer); }, []); return <section className="relative overflow-hidden bg-brand text-white dark:bg-[#101d32]"><div className="mx-auto grid max-w-[1440px] items-center gap-12 px-4 py-16 md:grid-cols-[1fr_0.8fr] md:px-8 md:py-24"><div className="max-w-2xl"><AnimatePresence mode="wait"><motion.div key={slide.key} initial={{ opacity: 0, x: -24 }} animate={{ opacity: 1, x: 0 }} exit={{ opacity: 0, x: 24 }} transition={{ duration: .45 }}><p className="text-sm text-white/70">We can help you find the right {slide.label.toLowerCase()}.</p><h1 className="mt-4 text-4xl font-bold leading-[1.04] tracking-[-0.04em] sm:text-5xl md:text-7xl">{slide.title}</h1><p className="mt-6 max-w-lg text-base leading-7 text-white/80 md:text-lg">{slide.copy}</p><div className="mt-8 flex flex-col gap-3 sm:flex-row"><Button href={slide.href} variant="secondary" size="lg">Explore {slide.label} <ArrowRight className="h-4 w-4" aria-hidden="true" /></Button><Link href="/contact" className="inline-flex min-h-13 items-center justify-center rounded-sm px-5 text-sm text-white/90 hover:bg-white/10 focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-white">Talk to a staff</Link></div></motion.div></AnimatePresence></div><div className="relative mx-auto w-full max-w-[420px]"><div className="absolute -inset-4 rotate-3 rounded-[2rem] border border-white/20" /><AnimatePresence mode="wait"><motion.div key={slide.key} initial={{ opacity: 0, x: 100, rotate: 8 }} animate={{ opacity: 1, x: 0, rotate: -3 }} exit={{ opacity: 0, x: -100, rotate: -8 }} transition={{ duration: .65, ease: "easeOut" }} className="relative overflow-hidden rounded-[1.5rem] border border-white/35 bg-white p-3 shadow-2xl"><div className="relative aspect-[4/5] overflow-hidden rounded-xl bg-[#dce4f4]"><img src="/products/mock.png" alt={slide.label + " collection"} className="h-full w-full object-cover" /></div><div className="flex items-center justify-between px-2 pb-1 pt-3 text-[#101d32]"><span className="text-xs font-semibold uppercase tracking-[.16em]">{slide.label}</span><span className="font-mono text-xs text-[#234f9b]">CIMALC / 0{active + 1}</span></div></motion.div></AnimatePresence></div></div><div className="mx-auto flex max-w-[1440px] gap-2 px-4 pb-8 md:px-8">{slides.map((item, index) => <button key={item.key} type="button" aria-label={"Show " + item.label} onClick={() => setActive(index)} className="h-2 rounded-full bg-white/30 focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-white" style={{ width: index === active ? 40 : 16, backgroundColor: index === active ? "#4f7cff" : undefined }} />)}</div></section>; }
